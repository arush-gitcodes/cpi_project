import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.elderlycare.app',
  appName: 'ECSS',
  webDir: 'dist/elderly_care/browser'
};

export default config;
